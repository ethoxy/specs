# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely need to edit after that.
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
title: Ethereum Classic Improvement Proposals
email: hi@that.world
description: >
  Collections of Ethereum Classic Improvement Proposals
baseurl: ""
url: "https://ecips.that.world"
permalink: pretty
author:
  name: Wei Tang
  email: hi@that.world

# Build settings
markdown: kramdown

plugins_dir: common/_plugins
includes_dir: common/_includes
sass:
  sass_dir: common/_sass

collections:
  specs:
    output: true
    permalink: /:name/

defaults:
  - scope:
      type: specs
    values:
      layout: spec
